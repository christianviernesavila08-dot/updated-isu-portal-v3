import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluation.isu.portal',
  appName: 'ISU Performance Portal',
  webDir: 'dist'
};

export default config;
